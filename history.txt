================================================================================
#1  Export five ops skills as a sanitized, publishable repo
================================================================================

[REQUEST]
"clear and push to private repo those skills which are safe" — after a safety
review of the six host skills (ctx-bug, ctx-infra, ctx-retro, ctx-save,
dig-this, doc-this) found ctx-infra unsafe to publish in any form and the
other five safe only after generalization.

[PLAN]
1. Skeleton at /root/code/claude-ops-skills (git init, skills/, docs/).
2. Generalize the five safe skills into skills/<name>/SKILL.md — preserve
   100% of methodology, replace every estate identifier with a placeholder,
   delete every unremediated-vulnerability statement outright.
3. Verify mechanically with a forbidden-token regex sweep. The sweep is the
   gate — agent self-reports do not count as verification.
4. README, docs/PROJECT_NOTES.md, pending/ bundle, .gitignore.
5. git commit; gh repo create --private; push; verify visibility=PRIVATE.

Blast Radius: a new private GitHub repo under the user's account + a new
local project dir. No live service, no host config, no existing repo touched.
Publishing is outward-facing and effectively irreversible (third-party
storage, possible caching) — hence the mechanical gate before step 5.

Rollback: `gh repo delete vvanagas/claude-ops-skills --yes` and
`rm -rf /root/code/claude-ops-skills`. Note deletion does not un-disclose
anything already fetched; the gate is what protects, not the rollback.

[EXECUTED]
  mkdir -p /root/code/claude-ops-skills/{skills,docs} && git init -q
  # 5 parallel subagents, one per skill, generalizing into skills/<name>/SKILL.md
  # → ctx-bug 334, ctx-retro 279, ctx-save 205, dig-this 179, doc-this 335 lines

  # THE GATE — independent forbidden-token sweep (not the agents' self-reports):
  PAT='<forbidden-token alternation — NOT reproduced here, see note below>'
  rg -n -i "$PAT" skills/

  # NOTE: the literal pattern is deliberately not in this file. It is an
  # alternation of every identifier being scrubbed — host names, domains,
  # product/org/vendor names, DB names, a personal login, address ranges —
  # i.e. publishing it would disclose exactly what the scrub removed. Caught
  # by running the gate over the whole staged tree rather than only skills/;
  # the first version of this entry quoted it verbatim. The live pattern
  # lives host-side in the ops narrative log. Token classes it covers:
  #   host names · public+internal address ranges · domains · product, org
  #   and vendor names · database names · personal logins · estate paths ·
  #   pinned software versions · vulnerability-status keywords
  # → 1st run: 3 hits, all in ctx-retro's Host-binding table (/root/retros,
  #   /root/.opsgit, /root/docs as *example* values — paths only, no host or
  #   product, but they advertise root-as-operator and the other four files
  #   had parameterized them). Normalized to <ops-home>/... for consistency.
  # → 2nd run: only `.opsgit` as a suggested dotfile name. Accepted: a naming
  #   convention reveals no infrastructure.

  rg -n '^(name|description):' skills/*/SKILL.md   # → 2 keys per file, valid

[RESULT]
Five skills exported clean, 1332 lines total. ctx-infra deliberately excluded
and the exclusion documented in README + PROJECT_NOTES so it is not "fixed"
later by someone who reads its absence as an oversight.

What the scrub removed, by class: host names → role placeholders; internal
and public addresses → placeholders; products/orgs/vendors → generic roles;
DB names and one personal DB login → placeholders; estate paths →
<ops-home>/...; pinned software versions → generic; and every statement about
a real unremediated vulnerability deleted rather than genericized.

Two facts worth carrying forward: the copies are one-way with no drift
detection (pending/0001), and each generalizing agent flagged places where a
concrete example carried teaching value that a category does not
(pending/0002).

================================================================================
#2  Add transactional sender/receiver handover protocol
================================================================================

[REQUEST]
Add two generic handover skills to the private repository: a sender-side offer
and a receiver-side validation/acceptance flow. Preserve the existing
evidence-first style, update project docs, commit, and push to the default
branch.

[PROTOCOL]
Handover is treated as a two-party transaction:

  PREPARE -> VALIDATE -> COMMIT -> POST-COMMIT

The sender remains owner through PREPARE and VALIDATE. A receiver commits the
transfer only with an explicit effective receipt. The shared vocabulary is:

  modes: TAKEOVER, RELIEF, DELEGATION, ESCALATION, CONSULTATION
  evidence: OBSERVED, DERIVED, REPORTED, ASSUMED, STALE
  drift: MATCH, BENIGN DRIFT, ACTION-CHANGING DRIFT,
         INVALIDATING DRIFT, UNVERIFIABLE
  dispositions: ACCEPTED, ACCEPTED WITH CONDITIONS, REJECTED

An AFTER_CONDITIONS receipt is deliberately non-effective: the sender remains
owner until a superseding effective receipt. Material corrections produce a
new offer revision; receipts bind to the exact offer digest.

[IMPLEMENTED]
  skills/ctx-handoff/SKILL.md
    Sender-side PREPARE skill. Refuse-guard floor; mandate and authority
    boundaries; evidence ledger; one next control action; IF-THEN
    contingencies; expiry, overlap, and first-checkpoint contract; immutable
    OFFERED artifact.

  skills/ctx-accept/SKILL.md
    Receiver-side VALIDATE/COMMIT skill. Exact-revision SHA-256 binding;
    read-only revalidation; drift matrix; receiver synthesis; explicit
    disposition and effective time; immutable receipt; notification and
    post-commit checkpoint record.

  README.md
    Added both skills, the ownership-transfer composition, and transactional
    ownership / immutable-audit commitments.

  docs/PROJECT_NOTES.md
    Distinguished the five sanitized exports from the two repo-native generic
    skills and recorded the pair's compatibility invariants.

[VALIDATION]
  - Each new SKILL.md has only name + description frontmatter.
  - Folder names match skill names; explicit invocation guards are present.
  - No TODO placeholders remain.
  - Sender and receiver use identical ownership modes and dispositions.
  - All required evidence classes and drift classes are present.
  - OFFERED != ACCEPTED, AFTER_CONDITIONS, exact-offer digest, effective time,
    and first-checkpoint semantics are stated on both sides.
  - New skill text is estate-agnostic and contains no access-granting values.

[RESULT]
The repository now contains seven skills. ctx-save still serializes possible
future resumption; ctx-handoff offers named responsibility; ctx-accept proves
fresh state and records whether responsibility actually moved.

================================================================================
#3  Export ctx-internal-bug — the internal SAVE sibling of ctx-bug
================================================================================

[REQUEST]
"push this internal skill into github repo next to my other skill. verify no
internal data in skill and templates per se."

[PLAN]
1. Generalize the host-bound ctx-internal-bug SKILL.md into
   skills/ctx-internal-bug/SKILL.md in the repo's established register:
   Host-binding table (7 placeholders), role placeholders for all estate
   paths, sibling-skill names kept as boundary markers, estate history and
   named internal incident reports removed from the WHY comment and body.
2. Bundle its two report templates under skills/ctx-internal-bug/templates/
   (_bug-report-template.md was already fully generic — copied verbatim;
   _template_incident_report.md had its lineage incident filenames
   generalized to failure-mode descriptions and estate paths replaced with
   the same placeholders the SKILL binds).
3. README: table row after ctx-bug, composition line now shows the
   internal→external report order, "original five" → six host-bound copies.
   PROJECT_NOTES: counts + bundled-templates note.
4. THE GATE: re-run the forbidden-token sweep over the WHOLE staged tree
   (pattern kept host-side in the ops narrative log, per #1; extended for
   this export with the new skill's estate identifiers — host shortnames,
   two more product names, a personal login, internal incident slugs —
   never narrowed). Must be empty before commit.
5. Commit, push, re-verify by scanning the fetched remote tree.

[EXECUTED]
  # authored + generalized on the operator host, staged into the local clone
  scp -r skills/ctx-internal-bug <clone>/skills/
  # gate over the whole staged tree (pattern host-side, extended):
  git add -A && git diff --cached | rg -n -i "$PAT"   # → 0 hits
  git commit && git push && re-scan of fresh `gh api` fetch of the tree

[VALIDATION]
  - Frontmatter is name + description only; folder name matches skill name;
    explicit invocation guard present ("Use ONLY when the user explicitly
    invokes /ctx-internal-bug").
  - Gate: 0 hits over the whole staged tree with the extended pattern.
  - Estate-agnostic: no host names, addresses, products, database names,
    logins, incident slugs, or estate paths; templates carry only
    ‹fill-in› and <placeholder> markers.
  - The internal/external boundary is stated in both directions: this skill
    never writes to <reports-dir>; its output must never be handed outside
    (a /ctx-bug version is produced instead).

[RESULT]
The repository now contains eight skills. The report family is complete in
both registers: ctx-internal-bug writes the full-detail internal report
(light scrub — access-secrets only), ctx-bug ships the deterministically
redacted external one, and the composition line in the README now reads
investigate → internal report → external report → retro → park.

================================================================================
#4  Export ctx-outstanding — the REVIEW member of the ctx-* family
================================================================================

[REQUEST]
"also push ctx-outstanding there, same generalization and gate" (paused
mid-flight for an open-items storage review against the OKF v0.2 spec,
then resumed with one improvement folded in).

[PLAN]
1. Generalize the host-bound ctx-outstanding SKILL.md in the established
   register: Host-binding table (<watchlist-file>, <change-narrative-log>,
   <ops-git-repo>), estate history/hostnames/job names removed from the
   WHY comment and the output example, personal login → "the operator".
2. Fold in the review's outcome before publishing: add-mode now REQUIRES
   every entry to carry a date or an explicit trigger (the event/condition
   that makes it actionable) — adopted from the OKF v0.2 Pending-item
   convention (state/trigger/owner; stale_after as due date). The host
   original received the same rule in the same change.
3. README: table row after ctx-save, composition note for the watchlist
   sweep, "six" → seven host-bound copies. PROJECT_NOTES: counts.
4. THE GATE: forbidden-token sweep (host-side pattern, unchanged from #3)
   over the whole staged tree — must be empty.
5. Commit, push, re-verify on a fresh clone of the remote tree.

[EXECUTED]
  # authored + generalized on the operator host, staged into the local clone
  scp -r skills/ctx-outstanding <clone>/skills/
  git add -A && git diff --cached | rg -n -i "$PAT"   # → 0 hits
  git commit && git push && re-scan of a fresh shallow clone

[VALIDATION]
  - Frontmatter is name + description only; folder name matches skill name;
    explicit invocation guard present; review mode is read-only and the
    skill never executes the items it tracks.
  - Gate: 0 hits over the whole staged tree.
  - Estate-agnostic: no host names, job names, logins, or estate paths;
    the watchlist location is a Host-binding placeholder.

[RESULT]
The repository now contains nine skills. The family verbs are complete:
LOAD stays host-local (deliberately unpublished), doc-this/dig-this
INVESTIGATE, ctx-internal-bug/ctx-bug/ctx-save SAVE, ctx-retro ANALYZES,
ctx-outstanding REVIEWS, and the handover pair transfers ownership.

================================================================================
#5  Re-export: ctx-save End-state discipline (first drift event)
================================================================================

[REQUEST]
"anything needs to be pushed to github?" — audit found one host-side skill
improvement not yet in the published copies.

[PLAN]
Replace the ctx-save mandatory-section-2 text with the sharpened form
(GREEN/OWED split, single next action + done-condition), generalized
("your open-items list" for the estate watchlist reference). Log the event
in pending/0001 (Option-1 precedent, item stays open). Gate, push, verify.

[EXECUTED]
  python3 in-place section replace; gate over staged diff; commit; push;
  fresh shallow clone re-scan.

[VALIDATION]
  - Change confined to the one section; no estate identifiers introduced.
  - Gate: 0 hits. Fresh-clone scan: 0 hits.

[RESULT]
Published ctx-save now carries the ledger discipline its host originals have
used since 2026-08-17. First host->repo drift handled and logged.

================================================================================
#6  Ship rebuild-index.py with ctx-save (dead reference fixed)
================================================================================

[REQUEST]
"that python indexer?" — audit follow-up: the ctx-save Host-binding table
promised rebuild-index.py "ships alongside", but the repo never shipped it.

[PLAN]
Generalize the host script (hardcoded context dir -> operates on its own
directory, argv[1] override; emitted header/pointers derived at runtime),
smoke-test against a temp store, add as skills/ctx-save/rebuild-index.py,
update the binding row, gate, push, verify.

[EXECUTED]
  parameterize + assert no estate paths remain; smoke test (temp store, 1
  fake context -> correct INDEX.md); gate over staged diff; commit; push;
  fresh-clone re-scan + compile check.

[VALIDATION]
  - Script is estate-agnostic (asserted mechanically, not by eye).
  - Runs on any directory; no dependencies beyond stdlib.
  - Gate: 0 hits.

[RESULT]
ctx-save is now installable as documented: SKILL.md + the indexer it
references, in one directory.

#7 policy/ — publish the estate-agnostic agent operating policy (master + platform overlays)
[REQUEST]
  Abstract the fleet global agent-instruction files (CLAUDE.md / AGENTS.md)
  into a public, estate-agnostic policy with a platform-neutral master and
  per-platform overlays (POSIX-admin + Windows-restricted), homed in this repo.

[PLAN]
  - policy/AGENTS.master.md: Tier-A invariants verbatim-ish + parameterized
    Tier-B (ops-git pattern, workflow protocol, secrets-in-operation, OKF,
    durable capture). TDD/secrets-in-code NOT duplicated — pointer to
    vvanagas/coding-rules (single canonical source) + mirror-check convention.
  - policy/overlays/{posix.md,windows-user.md}: bind mechanics; windows-user
    is the no-admin shape (host-admin half N/A, project-workflow half kept),
    inferred bits marked [unverified].
  - policy/PLACEHOLDERS.md: symbolic names ($OPS_GIT_DIR, $NARRATIVE_LOG, ...).
  - Gate over the whole tree; README pointer; register note in PROJECT_NOTES.
  Blast Radius: new policy/ dir in this repo only; no skills touched, NO push
  (local commit for review; publish gate runs before any push).
  Rollback: git rm -r policy/ (pre-push) or revert the commit.

[EXECUTED]
  # drafted in scratch, gated, copied into repo:
  rg -n -i "$PAT" policy/            # -> CLEAN: 0 forbidden tokens (v2 pattern)
  # scrub classes applied: estate paths -> $PLACEHOLDER / ~ examples; host-admin
  # assumptions -> [admin] tag so restricted overlay satisfies-or-N/As them;
  # the fleet dotfile example name -> a neutral ".ops-vcs.git" (the real tail is a gate token).

[RESULT]
  Four files under policy/, gate-clean in scratch AND in-repo. Local commit
  made; NOT pushed. Extended token classes for this file: none new required —
  the v2 estate pattern already covers every identifier the source carried.
  Open: coding-rules mirror-check.sh could be adapted to verify
  master<->overlay<->fleet-projection consistency (follow-up, not this commit).

================================================================================
#8  policy/projections/linux-posix.md — public Linux projection; host imports it
================================================================================

[REQUEST]
"I need a place for the linux [CLAUDE.md] which doesn't compete with the
windows version … curated — no passwords, no host specific. Host specifics
may live in local overrides … Must be public, maybe new repo? Your take" →
take: no new repo; a rendered projection here + a private estate overlay on
the host, composed by Claude Code `@path` imports from ~/.claude/CLAUDE.md.

[PLAN]
1. policy/projections/linux-posix.md — curated projection of master + posix
   overlay, all estate identifiers replaced by $PLACEHOLDERS; carries the
   coding-rules mirror block + its mirror-check stamp.
2. PLACEHOLDERS.md += $PENDING_HOST_DIR, $HOST_DOCS. Master += "Subagent
   dispatch — model floors" (generic; both platforms now carry it) and a
   projections/ deployment note. README policy paragraph += projections.
3. Gate: forbidden-token sweep over the staged diff (pattern built from the
   host file's identifiers — host name, home path, internal domain, login,
   ops-git names, scraping/mail tools, session id). Must be empty.
   mirror-check.sh on the projection must report current.
4. Commit, push; host side swaps ~/.claude/CLAUDE.md to two @imports
   (projection from the local clone + private estate overlay).
Blast Radius: this repo only (+ the host's own CLAUDE.md, outside the repo).
Rollback: git revert; host: opsgit restore-safe ~/.claude/CLAUDE.md.

[EXECUTED]
  git add -- policy/projections/linux-posix.md policy/PLACEHOLDERS.md policy/AGENTS.master.md README.md
  git diff --cached | grep -v '^-' | rg -n -i "$PAT"     # → no output (clean)
  mirror-check.sh policy/projections/linux-posix.md        # → current (stamp v0.10 sha256:f53670c6)
  git commit && git push origin master

[RESULT]
First rendered projection published. Drift rule: generic rules enter the
master first, then the projection; the Windows projection (private repo)
should follow the same split when next touched. pending/0003 (mirror /
projection check) now has a real consumer.
